name: CI Pipeline
on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches: 
      - main
      - develop

jobs:
  firstjod:
     runs-on: ubuntu-latest
     steps:
       - name: checkout
         uses: actions/checkout@v3

       - name: code
         run: echo "${{ github.ref }}"



         
